import { StyleSheet } from 'react-native';

export default StyleSheet.create({

    addButton: {
        width: 125,
        height: 125,
        alignSelf: 'center',
        justifyContent: 'space-around',
        marginBottom: 70,
    },

});
